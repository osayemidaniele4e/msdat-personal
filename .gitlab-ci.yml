image: "docker:20.10.16"

variables:
  DOCKER_TLS_CERTDIR: "/certs"

services:
  - docker:20.10.16-dind
  

stages:
#   - Test
  - Deploy

# Code Quality:
#   stage: Test
#   image: "node:16.3.0-alpine"
#   script:
#   - yarn install
#   - yarn run lint



Deploy Container:
 stage: Deploy
 only:
 - staging

 
 script:
 - mkdir -p ~/.ssh
 - echo "$MSDATDEVELOP" >> ~/.ssh/id_rsa
 - chmod 600 ~/.ssh/id_rsa
 - echo -e "Host *\n\tStrictHostKeyChecking no\n\n" > ~/.ssh/config
 - ssh -p3322  e4esysadmin@208.87.128.190 "cd /srv/msdat3.0/msdat/  && git pull origin staging && docker ps && docker-compose up -d --build && docker ps"

