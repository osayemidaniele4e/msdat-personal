export const formatFactor = (key) => {
  let displayFactor = '';
  switch (key) {
    case 'in percentage':
      displayFactor = '%';
      break;
    default:
      displayFactor = '';
      break;
  }
  return displayFactor;
};

export const format2 = (key) => {
  let displayFactor = '';
  switch (key) {
    case 'in percentage':
      displayFactor = '%';
      break;
    default:
      displayFactor = '';
      break;
  }
  return displayFactor;
};
